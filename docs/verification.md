# 验收记录 · 2026-09-21

## 完成情况

按确认的静态原型开发移动网页，未引入实时 3D 模型。页面中的标题、按钮、日记、计数和票券都是真实组件；角色、场景、事件和卡券使用内置 imagegen 生成的静态素材。

## 已执行检查

### 菜谱与旅行扩展

- `npm test`：25 / 25 通过。新增覆盖想吃清单顺序、做饭记录原子性/幂等、菜谱删除后的历史保留、编辑冲突、旅行大事件双向同步、旧数据迁移、未来计划不计入回顾、距离/地点/国家去重与年份筛选、坐标与日期校验、小红书元数据/结构化菜谱、受限页面补录、短链跳转边界及平台首页误识别防护。
- `npm run test:e2e`：11 / 11 通过。包含原有 7 项回归和新增菜谱识别草稿校正/排序/做过/收藏/持久化、地图标注与未来大事件双向跳转回写、五页 320px 布局、手动坐标和计划完成后转入回顾。
- TypeScript 和生产构建通过；新增 `/api/recipes/import` 与 `/api/places/search` 路由。
- 实际联网检查：通过应用 API 搜索 Toronto 返回 HTTP 200、3 个真实地点；Edge 成功加载 4 张 OpenStreetMap 瓦片，并显示署名。截图 `design/qa/map-live-service.png`。
- 小红书实际联网检查使用平台首页，验证不会将平台标题当作菜名。没有提供真实菜谱笔记，因此完整笔记识别使用公开元数据/JSON-LD 测试样本与浏览器可编辑草稿验证，不能据此保证任意真实笔记均可提取。
- 浏览器流程中小红书识别和地点搜索响应使用固定测试样本，避免依赖外部服务波动；真实服务连通性由独立 `scripts/smoke-lifestyle.mjs` 和 `scripts/smoke-map.mjs` 检查。测试记录使用独立目录，未添加到用户小窝。
- 云端 PostgreSQL 的地理编码缓存和事务限速路径已实现，仍需真实云端部署配置后验证。

### 原版实现的验收记录

- `npm test`：13 / 13 通过。覆盖时间券扣减、剩余分钟、请求重试幂等、专属对象权限、用尽和过期拦截、时区和夏令时、闰日周年、日记编辑冲突、输入校验、互动读取权限、会话签名和干净生产数据初始化。
- `npm run test:e2e -- --max-failures=1`：7 / 7 通过，Microsoft Edge，390×844 手机视口。覆盖状态更新、三个主页面、照片上传/持久化/编辑、卡片创建/兑换/历史、不同身份 API、同一与不同请求 ID 的并发扣减、倒计时置顶、双浏览器互动接收、断网输入保留，以及 320px 与 1440px 布局。
- 互动视觉反馈补充后，重新执行 `npm run test:e2e -- --grep "two browser sessions"`：1 / 1 通过。验证发送方和接收方都显示抱抱反馈，弹层内能看到断网错误，输入不会丢失。
- 最终 `npm run typecheck` 和 `npm run build` 通过；生产构建无警告。
- 静态插画透明通道通过 Sharp metadata 检查，最终效果通过真实页面截图检查。
- 生产服务端跟踪清单中 `.local` 与 `design` 私有/设计文件引用数为 0；另以 `.vercelignore` 排除本地数据和环境文件。

## 视觉截图

位于 `design/qa/`：

- home-mobile.png：初始小窝
- home-updated-mobile.png：切换健身状态后的角色场景
- status-mobile.png：状态和心情选择
- journal-mobile.png：日记和倒计时
- wallet-mobile.png：叠卡卡包
- event-form-mobile.png：图片日记表单
- redeem-mobile.png：使用前后次数/分钟确认
- hug-feedback-mobile.png：互动视觉反馈
- wallet-320.png：窄屏布局
- home-desktop.png：桌面布局
- recipes-mobile.png：菜谱想吃清单与顺序操作
- trips-mobile.png：旅行回顾、地图区域和行程列表
- map-live-service.png：真实在线地图底图显示

## 运行和部署状态

本地地址：http://localhost:3006 。第一次进入可直接选择身份。正式预览数据使用 `.local/home`，测试使用 `.local/e2e-*` 独立目录。

2026-09-21 已创建 Vercel 项目并关联 GitHub main；首个生产部署状态 READY，正式域名 https://loo-kingdom.vercel.app 的会话接口返回 200。Neon 真实连接已验证。上述浏览器回归在隔离本地数据中运行，不等同于真人手机外网验收。

现有静态艺术对应 12 种角色活动、6 个事件主题和 8 个蓝红卡券插画；任意自定义事件/卡片复用用户选择的主题插画，不在每次填写文字时调用 AI 生成图片。

## 双人卡券与时区扩展

本轮新增申请、预留、接受、计时、到期结算、取消与实物记录双人核对；旧兑换记录保留，旧 coupon.redeem 命令也会进入申请流程。测试环境显式清空数据库和真实口令配置，测试数据与云端数据库隔离。生产构建成功，32 项业务测试通过，14 项浏览器用例全部报告通过。首次提交检查确认环境文件、私人记录和本地截图未进入 Git 索引，并扫描了暂存文件中是否包含真实密钥。

## 2026-09-22 交互修正

- 发卡的服务端和表单均限定接收者为对方；校验先于请求去重，重放也不能绕过。旧持有卡片保留。
- 大事件两个分类全部使用一致的缩略列表，点击弹出详情；首页置顶倒计时保留。
- 足迹总览每段旅程仅绘制首站标记，不绘制城市线路；详情保留全部地点及路线。统计仍使用完整行程。
- 卡包后两层按当前持有列表循环渲染真实下一张卡的颜色、插画、标题和余额；一张卡无后层，两张卡一层，三张以上两层。
- 新增干饭中（活动索引 6）和 Loo心愿兑现券（插画索引 4），旧索引稳定。心愿券默认实物兑现流程，可填写相机或手环等礼物及规格、价格、照片。
- 小红书新增序列化笔记正文解析，按当前笔记 ID 取正文，不执行页面脚本；总请求时间限制 15 秒，分别提示受限、失效、超时、网络和跳转异常。分享文案仍可辅助识别；未收到实际失败笔记样例，不保证受限页面可导入。
- 类型检查与生产构建通过，36 项业务测试通过。
- 原有 14 项手机端回归通过；新增 3 项用例在隔离登录计数、等待登录完成后全部通过。额外复验路线弹窗截图 1 / 1 通过。覆盖自发卡 403、实物心愿模板、动态堆叠与单卡无后层、干饭刷新持久性、两个事件分类的统一缩略行，以及两旅程总览 2 标记 / 四城详情 4 标记与 1 条线路。
